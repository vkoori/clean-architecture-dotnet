i18n
shared campaign
observer (saving, saved, updating, updated, ...)
fluent api (click house)
fix exception handling
autoMapper
fix cors
fix audit log
add sample event (after action call some events)
